{
  "name": "keyframe-resample",
  "version": "0.0.0",
  "description": "keyframe-resample",
  "type": "module",
  "sideEffects": false,
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "main": "./dist/keyframe-resample.cjs",
  "module": "./dist/keyframe-resample.esm.js",
  "exports": {
    "require": "./dist/keyframe-resample.cjs",
    "default": "./dist/keyframe-resample.modern.js"
  },
  "repository": "github:donmccurdy/keyframe-resample",
  "author": "Don McCurdy <dm@donmccurdy.com>",
  "license": "MIT",
  "scripts": {
    "dist": "yarn build && yarn asbuild",
    "build": "microbundle --format cjs,esm,modern --no-compress --define PACKAGE_VERSION=$npm_package_version",
    "watch": "microbundle watch --format cjs,esm,modern --no-compress --define PACKAGE_VERSION=$npm_package_version",
    "test": "ava test/*.ts",
    "coverage": "c8 --reporter=lcov --reporter=text ava test/*.ts --tap",
    "coverage:report": "c8 report --reporter=text-lcov > coverage/coverage.lcov",
    "preversion": "yarn dist && yarn test",
    "version": "rimraf dist/* && yarn dist && git add -u",
    "postversion": "git push && git push --tags && npm publish && yarn coverage:report",
    "asbuild:debug": "asc assembly/index.ts --target debug",
    "asbuild:release": "asc assembly/index.ts --target release",
    "asbuild": "npm run asbuild:debug && npm run asbuild:release",
    "start": "npx serve ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "5.59.0",
    "assemblyscript": "^0.27.3",
    "ava": "5.2.0",
    "eslint": "8.39.0",
    "eslint-config-prettier": "8.8.0",
    "microbundle": "0.15.1",
    "prettier": "2.8.8",
    "rimraf": "5.0.0",
    "ts-node": "10.9.1",
    "typescript": "5.0.4"
  },
  "files": [
    "dist/",
    "src/",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "browserslist": [
    "defaults",
    "not IE 11",
    "node >= 14"
  ],
  "ava": {
    "extensions": {
      "ts": "module"
    },
    "nodeArguments": [
      "--loader=ts-node/esm"
    ]
  },
  "dependencies": {
    "c8": "^7.11.3"
  }
}